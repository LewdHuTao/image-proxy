module.exports = {
    BEARER_KEY: "lewdhutao", // Bearer key is require to upload file
    PRODUCTION: false, // If set to false it will use localhost:3000, If set to true it will use domain name
    PORT: 3000, // Default port use for express server
    DOMAIN: "", // Need to use cloudflare tunnel or any other service that can assign custom domain
}